# mini-sklearn
mini-sklearn: NumPy implementations of core ML algorithms (linear/ridge regression, logistic regression, k-means, gradient descent) built from scratch, with tests and docstrings, to understand the math behind scikit-learn's abstractions.
